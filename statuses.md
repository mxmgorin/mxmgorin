# GitHub statuses

Terminal / low-level / retro flavored statuses for the GitHub profile
("Set status" — emoji + up to ~80 chars, optional "Busy" toggle).

## ⭐ Top picks

- 🖥️ `~$ while true; do build; break_it; learn; done`
- 🔁 `~$ trap 'learn' ERR`
- 🦀 `~$ .unwrap() // what could go wrong`
- 🕹️ `~$ writing emulators nobody asked for`
- ☕ `~$ dd if=/dev/coffee of=/dev/brain`
- 🌱 `~$ touch grass # command not found`

---

## Shell one-liners

### Loops & control flow
- 🖥️ `~$ while true; do build; break_it; learn; done`
- 🖥️ `~$ while :; do code; coffee; repeat; done`
- 🔁 `~$ while alive; do learn; done`
- 🔁 `~$ trap 'learn' ERR`
- 🧨 `~$ trap '' SIGINT # can't stop me`
- ⚙️ `~$ set -e # no more silent failures`
- 🐛 `~$ set -x # narrate everything`
- 🌙 `~$ nohup passion & disown`
- 🔂 `~$ watch -n1 make`
- ⏫ `~$ renice -20 $(pidof focus)`
- 🛑 `~$ kill -STOP procrastination`
- ☕ `~$ yes | make progress`
- 🧵 `~$ xargs -P8 build`
- 🌒 `~$ shutdown -c # one more feature`
- 🔄 `~$ reboot # tried off and on again`
- 🔁 `~$ sudo !! # now with power`
- 🧹 `~$ history -c # what mistakes`

### git
- 📝 `~$ git commit -m "works, don't ask why"`
- 🔥 `~$ git push --force reality`
- 🕵️ `~$ git blame $(whoami)`
- 🌿 `~$ git checkout -b maybe-this-time`
- 🧪 `~$ git stash # future me's problem`
- ♻️ `~$ git rebase -i my-life`
- 🏷️ `~$ git tag v0.∞`

### build / ship
- 🦀 `~$ cargo build --release && pray`
- 🚀 `~$ ./configure && make && make regret`
- 🔧 `~$ if it_compiles; then ship; fi`

### Debug & systems
- 🧠 `~$ sudo systemctl restart brain.service`
- 📟 `~$ tail -f /var/log/curiosity`
- 🐛 `~$ gdb --args ./life`
- 💧 `~$ valgrind ./me # leaks found`
- 💥 `~$ dmesg | grep -i panic`
- 🔍 `~$ strace $(whoami)`
- 🧠 `~$ objdump -d ~/brain`
- 📈 `~$ perf record ./today`
- 🩺 `~$ fsck ~/life # errors found`

### Files & disk
- 🗑️ `~$ rm -rf ~/doubts`
- 💽 `~$ df -h ~/patience # 2% left`
- 🗂️ `~$ du -sh ~/regrets`
- 🔗 `~$ ln -s coffee ~/energy`
- 🔎 `~$ find / -name motivation 2>/dev/null`
- 💾 `~$ man life | grep purpose`
- 🕳️ `~$ echo problems > /dev/null`
- 🌱 `~$ touch grass # command not found`

### Networking
- 📡 `~$ ping -i 60 mom # still alive`
- 🕳️ `~$ iptables -A INPUT -j DROP distractions`
- 🔌 `~$ nc -lvp 1337 # waiting`
- 🧭 `~$ traceroute happiness`
- 🚀 `~$ ssh prod # yolo`

### Containers / cloud
- 📦 `~$ docker run --rm chaos`
- 📉 `~$ kubectl scale me --replicas=0`
- 🤞 `~$ terraform apply # fingers crossed`

### vim (the eternal struggle)
- 🚪 `~$ :wq # if only leaving were this easy`
- ⏳ `~$ vim +q # 4 hours later`
- 🙅 `~$ :q! # abandon all changes`
- ⌨️ `~$ vim ~/.config forever`

### SQL
- 🗑️ `~$ DROP TABLE doubts;`
- 📋 `~$ SELECT * FROM ideas WHERE done=0`
- ✅ `~$ COMMIT; -- no rollback`

### Package managers
- 🐧 `~$ pacman -S motivation`
- 🐧 `~$ pacman -Syu # living on the edge`
- 📥 `~$ npm install universe # 4gb`
- 🐄 `~$ apt moo`
- ☕ `~$ cargo add caffeine`
- 🖥️ `~$ neofetch # 0 regrets`

### Hacker misc
- ☕ `~$ dd if=/dev/coffee of=/dev/brain`
- 💣 `~$ :(){ :|:& };:`
- 🌍 `~$ echo "hello, world" # still magic`

## Language-flavored

### Rust
- 🦀 `~$ loop { emulate(); render(); }`
- 🦀 `~$ .unwrap() // what could go wrong`
- 🦀 `~$ #[derive(Debug)] struct Me;`
- 🦀 `~$ tokio::spawn(chaos)`
- 🦀 `~$ Rc<RefCell<Sanity>>`
- 🦀 `~$ cargo clippy # ok fine, you're right`
- ⚠️ `~$ unsafe { trust_me() }`
- 💢 `~$ panic!("not again")`
- 🧩 `~$ match mood { _ => keep_going() }`

### Go
- 🐹 `~$ go build ./... && go home`
- 🐹 `~$ defer cleanup()`
- 🐹 `~$ go test -race # scary`

### C#
- 🔷 `~$ await Task.WhenAll(dreams)`
- 🔷 `~$ async { forever }`

## Low-level / emulators

- 🕹️ `~$ fetch; decode; execute; repeat`
- ⚙️ `~$ jmp $ # infinite loop`
- 💤 `~$ nop; nop; nop; # thinking`
- 📟 `~$ cpu.step() // 4 cycles`
- 🔺 `~$ segfault at 0xDEADBEEF`
- 🧼 `~$ xor eax, eax # start fresh`
- ⛔ `~$ hlt # end of day`
- 🔂 `~$ tick(); tick(); tick();`
- 💾 `~$ save state -> slot 3`

## Project-specific

- 🎮 `~$ ./oxgbc --rom tetris.gb`
- 👾 `~$ chip8 run pong.ch8`
- 🌐 `~$ retsurf --console rg35xx`

## Non-shell (plain vibe)

### Terminal / CRT
- 🟢 `booting… phosphor warming up`
- ⌨️ `living in the terminal`
- 📟 `> compiling reality`

### Low-level / emulators
- 🕹️ `writing emulators nobody asked for`
- 🔧 `reverse-engineering the machine, one opcode at a time`
- 🧩 `"can I build this myself?" — yes, again`
- 💾 `down in the bytes`

### Rust / systems
- 🦀 `fighting the borrow checker (winning, mostly)`
- ⚙️ `chasing microseconds`
- 🚀 `if it compiles, ship it`

### Linux / retro gaming
- 🐧 `ricing configs instead of sleeping`
- 👾 `emulating the '90s`
- 🎮 `handheld console collection: still growing`

### Playful hacker
- 😎 `probably in a debugger`
- ☕ `turning coffee into segfaults`
- 🌙 `low-level by day, lower-level by night`
